package Interfaces2;

class Main {

	public static void main(String[] args) {
		FixedStack ob = new FixedStack(5);
		FixedStack ob1 = new FixedStack(8);
		
		for(int i = 5; i <10 ; i ++ ) ob.push(i);
		for(int i=0; i<8; i++) ob1.push(i);
		
		
		 System.out.println("Stack in my stack1:  ");
				for(int i = 5; i < 10 ; i ++ ) ;
				 System.out.println("Stack in my stack1:  " + ob.pop());
		 System.out.println("Stack in my stack2:  ");
			      for(int i=0; i<8; i++);
			      System.out.println("Stack in my stack1:  " + ob1.pop());

	}

}
